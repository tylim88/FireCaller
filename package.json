{
	"name": "firecaller",
	"version": "1.0.0",
	"description": "🔥Write callable functions systematically like a Firelord. No more chaotic error handling, no more unsafe endpoint data type, no more messy validation. Be the Master of Fire you always wanted to be.",
	"source": "src/index.ts",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"targets": {
		"types": false,
		"main": {
			"optimize": true
		}
	},
	"scripts": {
		"reinstall": "rm -rf node_modules && rm -rf package-lock.json && npm i && npx husky install",
		"tsc": "tsc",
		"lint": "eslint . --ext .ts --fix",
		"pretty-quick": "pretty-quick --stage",
		"test": "firebase emulators:exec --only functions \"rm -rf coverage && jest --forceExit\"",
		"parcel": "npm un firebase && npm i firebase && rm -rf dist && tsc --emitDeclarationOnly true && parcel build && npm i -D firebase",
		"build": "rm -rf dist && tsc",
		"link": "npm link && npm link firelordjs",
		"pub": "npm run build && npm publish --tag beta",
		"dev": "=====================================dev========================================",
		"d-test": "tsc && npm --prefix functions run build && npm test"
	},
	"devDependencies": {
		"@babel/preset-env": "^7.17.10",
		"@babel/preset-typescript": "^7.16.7",
		"@parcel/transformer-typescript-types": "^2.3.2",
		"@types/jest": "^27.0.2",
		"@types/lodash": "^4.14.180",
		"@types/shelljs": "^0.8.11",
		"@typescript-eslint/eslint-plugin": "^5.0.0",
		"@typescript-eslint/parser": "^5.0.0",
		"dotenv": "^16.0.0",
		"eslint": "^7.32.0",
		"eslint-cli": "^1.1.1",
		"eslint-config-prettier": "^8.3.0",
		"eslint-import-resolver-typescript": "^2.5.0",
		"eslint-plugin-import": "^2.25.2",
		"eslint-plugin-prettier": "^4.0.0",
		"firebase": "^9.7.0",
		"husky": "^7.0.0",
		"jest": "^27.2.5",
		"jsdoc": "^3.6.5",
		"parcel": "^2.3.2",
		"prettier": "^2.4.1",
		"pretty-quick": "^3.1.3",
		"shelljs": "^0.8.5",
		"ts-node": "^10.4.0",
		"typescript": "^4.6.2",
		"zod": "^3.15.1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/tylim88/FireCaller.git"
	},
	"keywords": [
		"firebase",
		"cloud",
		"functions",
		"helper"
	],
	"author": "",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/tylim88/FireCaller/issues"
	},
	"homepage": "https://github.com/tylim88/FireCaller#readme"
}
